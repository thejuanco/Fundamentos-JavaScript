//querySelector all 
//retorna todos los elementos que cumplan la condicion del selector 

const card = document.querySelectorAll('.card');
console.log(card); 

const formulario = document.querySelectorAll('.formulario');
console.log(formulario);

//Sino existe un documento 
const noExiste = document.querySelectorAll('.noExiste');
console.log(noExiste); //Retorna un elemento vacio 
