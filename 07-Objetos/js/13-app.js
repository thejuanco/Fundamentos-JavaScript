const producto = {
    nombre: 'Monitor de 20 pulgadas',
    precio : 300, 
    disponible: true
}

//Este comando nos permite conocer las llaves del objeto 
console.log(Object.keys(producto))

//Este comando nos permite cononcer los valores del objeto (parte derecha)
console.log(Object.values(producto))